const AUTO_SCAN_KEY='tradenex_auto_scan';
const qs=(s)=>document.querySelector(s);
function setAutoScanButton(enabled){const b=qs('#autoScanBtn');if(!b)return;b.textContent=enabled?'⚡ AUTO SCAN · AN':'⏸ AUTO SCAN · AUS';b.className=enabled?'primary-btn':'ghost-btn';b.dataset.enabled=String(enabled);b.title=enabled?'Automatische Analyse aktiv':'Automatische Analyse pausiert'}
async function refreshAutoScan(){try{const r=await fetch('/demo/status',{cache:'no-store'});const s=await r.json();const enabled=!!s.enabled;localStorage.setItem(AUTO_SCAN_KEY,String(enabled));setAutoScanButton(enabled)}catch{const saved=localStorage.getItem(AUTO_SCAN_KEY);setAutoScanButton(saved!=='false')}}
async function toggleAutoScan(){const b=qs('#autoScanBtn');const current=b?.dataset.enabled==='true';try{const r=await fetch('/demo/toggle',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({enabled:!current})});const d=await r.json();if(!r.ok)throw Error(d.detail||'Auto Scan konnte nicht geändert werden');localStorage.setItem(AUTO_SCAN_KEY,String(!!d.enabled));setAutoScanButton(!!d.enabled);const t=qs('#toast');if(t){t.textContent=d.enabled?'⚡ Auto Scan aktiviert':'⏸ Auto Scan pausiert';t.classList.add('show');setTimeout(()=>t.classList.remove('show'),1800)}}catch(e){const t=qs('#toast');if(t){t.textContent=e.message;t.classList.add('show');setTimeout(()=>t.classList.remove('show'),2200)}}}
function loadAsset(href){return new Promise(resolve=>{if(document.querySelector(`link[href="${href}"]`))return resolve();const l=document.createElement('link');l.rel='stylesheet';l.href=href;l.onload=resolve;l.onerror=resolve;document.head.appendChild(l)})}
function loadScript(src){return new Promise(resolve=>{if(document.querySelector(`script[src="${src}"]`))return resolve();const s=document.createElement('script');s.src=src;s.onload=resolve;s.onerror=resolve;document.head.appendChild(s)})}
async function loadTheme(){if(window.__TRADENEX_THEME_LOADED)return;window.__TRADENEX_THEME_LOADED=true;await loadAsset('./theme.css?v=5');await loadAsset('./design-v3.css?v=2');await loadAsset('./ambience-engine.css?v=2');await loadScript('./theme.js?v=4');await loadScript('./ambience-engine-v3.js?v=1')}
function initAutoScan(){if(qs('#autoScanBtn')){qs('#autoScanBtn').onclick=toggleAutoScan;refreshAutoScan();setInterval(refreshAutoScan,2000)}loadTheme()}
document.addEventListener('DOMContentLoaded',initAutoScan);