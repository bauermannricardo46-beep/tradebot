(()=>{
const KEY='tradenex_visual_settings_v3';
const $=s=>document.querySelector(s);
const clamp=(v,a,b)=>Math.max(a,Math.min(b,v));
let canvas,ctx,W,H,dpr=1,raf=0,parts=[],stars=[],rain=[],nodes=[];
const extra=[['quantum-web','Quantum Web','Leuchtendes Netzwerk'],['energy-tunnel','Energy Tunnel','Warp-Tunnel'],['singularity','Singularity Core','Schwarzes Energiezentrum'],['galaxy-drift','Galaxy Drift','Parallax-Sternenstrom'],['cyber-city','Cyber City','Futuristische Skyline'],['ai-neural','AI Neural','Neuronales Daten-Netz'],['data-core','Data Core','Holografischer Datenkern'],['holo-rings','Holo Rings','Räumliche Hologramm-Ringe'],['electric-vortex','Electric Vortex','Elektrischer Wirbel'],['deep-space','Deep Space','Tiefer Sternennebel'],['digital-ocean','Digital Ocean','Leuchtendes Wellenfeld'],['particle-storm','Particle Storm','Dynamischer Partikelsturm'],['hex-flux','Hex Flux','Energie-Hexmesh'],['reactor','Reactor Core','Kinetischer Reaktor']];
function state(){try{return JSON.parse(localStorage.getItem(KEY)||'{}')}catch{return{}}}
function rgb(h){h=(h||'#55e7ff').replace('#','');return h.length===6?[parseInt(h.slice(0,2),16),parseInt(h.slice(2,4),16),parseInt(h.slice(4,6),16)]:[85,231,255]}
function rgba(h,a){const [r,g,b]=rgb(h);return`rgba(${r},${g},${b},${a})`}
function cs(){const s=getComputedStyle(document.documentElement);return[s.getPropertyValue('--theme-accent').trim()||'#55e7ff',s.getPropertyValue('--theme-accent-2').trim()||'#8c5cff',s.getPropertyValue('--theme-accent-3').trim()||'#ff39d1',s.getPropertyValue('--theme-metal').trim()||'#c8d0df']}
function seed(){const n=clamp(Math.floor(W*H/18000),90,240);parts=Array.from({length:n},()=>({x:Math.random()*W,y:Math.random()*H,z:.2+Math.random()*.8,vx:(Math.random()-.5)*.22,vy:(Math.random()-.5)*.22,r:.5+Math.random()*2}));stars=Array.from({length:Math.max(90,Math.floor(W*H/12000))},()=>({x:Math.random()*W,y:Math.random()*H,z:.15+Math.random()*.85,r:.4+Math.random()*1.5,v:.2+Math.random()*.7}));rain=Array.from({length:Math.max(42,Math.floor(W/22))},()=>({x:Math.random()*W,y:Math.random()*H,len:10+Math.random()*28,v:2+Math.random()*8}));nodes=Array.from({length:Math.max(32,Math.floor(W*H/23000))},()=>({x:Math.random()*W,y:Math.random()*H,vx:(Math.random()-.5)*.4,vy:(Math.random()-.5)*.4}))}
function resize(){if(!canvas)return;dpr=Math.min(devicePixelRatio||1,1.5);W=innerWidth;H=innerHeight;canvas.width=W*dpr;canvas.height=H*dpr;ctx.setTransform(dpr,0,0,dpr,0,0);seed()}
function dot(x,y,r,c,a){const g=ctx.createRadialGradient(x,y,0,x,y,r*5);g.addColorStop(0,rgba(c,a));g.addColorStop(1,'rgba(0,0,0,0)');ctx.fillStyle=g;ctx.beginPath();ctx.arc(x,y,r*5,0,Math.PI*2);ctx.fill();ctx.fillStyle=rgba(c,a);ctx.beginPath();ctx.arc(x,y,r,0,Math.PI*2);ctx.fill()}
function particleField(a,b,c,t){for(const p of parts){p.x+=p.vx*(1+p.z*2);p.y+=p.vy*(1+p.z*2);if(p.x<0)p.x=W;if(p.x>W)p.x=0;if(p.y<0)p.y=H;if(p.y>H)p.y=0;dot(p.x,p.y,p.r*p.z,p.z>.65?a:p.z>.4?b:c,.25*p.z)}}
function starsField(a,b,t,warp=false){for(const s of stars){s.y+=s.v*(warp?7:1);if(s.y>H+4)s.y=-4;ctx.fillStyle=rgba(s.z>.6?b:a,.2+.5*s.z);ctx.beginPath();ctx.arc(s.x,s.y,s.r*s.z,0,Math.PI*2);ctx.fill();if(warp&&s.z>.7){ctx.strokeStyle=rgba(a,.14);ctx.beginPath();ctx.moveTo(s.x,s.y);ctx.lineTo(s.x-(s.x-W/2)*.03,s.y-16);ctx.stroke()}}}
function rainField(a,b){ctx.font='10px monospace';for(const r of rain){r.y+=r.v;if(r.y>H+r.len*10){r.y=-20;r.x=Math.random()*W}for(let i=0;i<r.len;i++){ctx.fillStyle=rgba(i%8? a:b,.35*(1-i/r.len));ctx.fillText(String.fromCharCode(0x30a0+Math.floor(Math.random()*96)),r.x,r.y-i*9)}}}
function grid(a,b){const hy=H*.6;for(let x=-W;x<W*2;x+=46){ctx.strokeStyle=rgba(a,.14);ctx.beginPath();ctx.moveTo(W/2+(x-W/2)*.06,hy);ctx.lineTo(x,H);ctx.stroke()}for(let i=1;i<18;i++){const y=hy+Math.pow(i/18,1.65)*(H-hy);ctx.strokeStyle=rgba(b,.07+i*.004);ctx.beginPath();ctx.moveTo(0,y);ctx.lineTo(W,y);ctx.stroke()}}
function aurora(a,b,c,t){for(let i=0;i<6;i++){const x=W*(.1+i*.16)+Math.sin(t*.00035+i)*W*.1,y=H*(.2+i*.1)+Math.cos(t*.0003+i)*H*.08,g=ctx.createRadialGradient(x,y,0,x,y,Math.max(W,H)*.3);g.addColorStop(0,rgba(i%2?a:b,.12));g.addColorStop(.5,rgba(c,.045));g.addColorStop(1,'rgba(0,0,0,0)');ctx.fillStyle=g;ctx.fillRect(0,0,W,H)}}
function vortex(a,b,t){const cx=W/2,cy=H/2;for(let i=0;i<950;i++){const q=i/950,ang=i*.22+t*.0006*(i%3?1:-1),r=q*Math.min(W,H)*.5,x=cx+Math.cos(ang)*r*(.25+.8*q),y=cy+Math.sin(ang)*r*.55;ctx.fillStyle=rgba(i%2?a:b,.075);ctx.fillRect(x,y,1.5,1.5)}const g=ctx.createRadialGradient(cx,cy,0,cx,cy,Math.min(W,H)*.24);g.addColorStop(0,'rgba(0,0,0,.95)');g.addColorStop(.75,rgba(a,.08));g.addColorStop(1,'rgba(0,0,0,0)');ctx.fillStyle=g;ctx.beginPath();ctx.arc(cx,cy,Math.min(W,H)*.27,0,Math.PI*2);ctx.fill()}
function radar(a,b,t){const cx=W/2,cy=H/2,r=Math.min(W,H)*.4;for(let i=1;i<=4;i++){ctx.strokeStyle=rgba(a,.08);ctx.beginPath();ctx.arc(cx,cy,r*i/4,0,Math.PI*2);ctx.stroke()}const ang=t*.0007;ctx.strokeStyle=rgba(a,.22);ctx.lineWidth=1.3;ctx.beginPath();ctx.moveTo(cx,cy);ctx.lineTo(cx+Math.cos(ang)*r,cy+Math.sin(ang)*r);ctx.stroke();for(let i=0;i<7;i++){const q=ang-i*.5,rr=r*(.25+((i*29)%70)/100);dot(cx+Math.cos(q)*rr,cy+Math.sin(q)*rr,2.2,b,.4)}}
function network(a,b,c){for(const n of nodes){n.x+=n.vx;n.y+=n.vy;if(n.x<0||n.x>W)n.vx*=-1;if(n.y<0||n.y>H)n.vy*=-1}for(let i=0;i<nodes.length;i++)for(let j=i+1;j<nodes.length;j++){const p=nodes[i],q=nodes[j],d=Math.hypot(p.x-q.x,p.y-q.y);if(d<180){ctx.strokeStyle=rgba(d<95?a:b,(1-d/180)*.13);ctx.beginPath();ctx.moveTo(p.x,p.y);ctx.lineTo(q.x,q.y);ctx.stroke()}}nodes.forEach((n,i)=>dot(n.x,n.y,1.6,i%3?b:c,.35))}
function core(a,b,c,t){const cx=W/2,cy=H/2;for(let i=0;i<9;i++){const r=44+i*29;ctx.strokeStyle=rgba(i%2?a:b,.09);ctx.beginPath();ctx.arc(cx,cy,r,t*.0004*(i%2?-1:1),t*.0004*(i%2?-1:1)+Math.PI*(1.05+i*.08));ctx.stroke()}for(let i=0;i<14;i++){const ang=t*.001*(i%2?-1:1)+i*.45,r=70+i*7;dot(cx+Math.cos(ang)*r,cy+Math.sin(ang)*r,2.1,c,.32)}dot(cx,cy,3,c,.7)}
function waves(a,b,t){for(let k=0;k<8;k++){ctx.beginPath();for(let x=0;x<=W;x+=10){const y=H*.59+k*17+Math.sin(x*.011+k+t*.0012)*(10+k*1.8)+Math.sin(x*.003-t*.0006)*(18+k*2);x?ctx.lineTo(x,y):ctx.moveTo(x,y)}ctx.strokeStyle=rgba(k%2?a:b,.08+k*.007);ctx.stroke()}}
function city(a,b,t){const hy=H*.73;for(let i=0;i<24;i++){const x=i*(W/24),h=(.12+((i*17)%100)/100*.48)*H,w=W/24*.7;ctx.fillStyle=rgba(i%2?a:b,.07);ctx.fillRect(x,hy-h,w,h);for(let y=hy-h+12;y<hy-8;y+=12)if((i*11+y|0)%41<17){ctx.fillStyle=rgba(a,.16);ctx.fillRect(x+4,y,Math.max(2,w*.08),2)}}for(let i=0;i<9;i++){const y=hy-i*24+Math.sin(t*.0007+i)*5;ctx.strokeStyle=rgba(a,.08);ctx.beginPath();ctx.moveTo(0,y);ctx.lineTo(W,y);ctx.stroke()}}
function hex(a,b){const s=60;for(let y=-s;y<H+s;y+=s*.86)for(let x=-s;x<W+s;x+=s*1.5){const xx=x+(Math.floor(y/(s*.86))%2)*s*.75;ctx.strokeStyle=rgba((Math.floor(x/s)+Math.floor(y/s))%2?a:b,.08);ctx.beginPath();for(let i=0;i<6;i++){const ang=Math.PI/3*i,px=xx+Math.cos(ang)*s*.42,py=y+Math.sin(ang)*s*.42;i?ctx.lineTo(px,py):ctx.moveTo(px,py)}ctx.closePath();ctx.stroke()}}
function render(t){
 try{const st=state();const eff=st.background||document.body.dataset.ambience||'grid';const strength=clamp(Number(st.effectStrength??65)/100,0,1);const motion=st.motion!==false;const[a,b,c,d]=cs();ctx.clearRect(0,0,W,H);ctx.fillStyle='rgba(1,3,9,.82)';ctx.fillRect(0,0,W,H);ctx.globalAlpha=strength;ctx.globalCompositeOperation='lighter';
 if(eff==='grid'||eff==='synthwave')grid(a,b);if(eff==='matrix'||eff==='rain')rainField(a,b);if(eff==='particles'||eff==='particle-storm')particleField(a,b,c,t);if(eff==='stars'||eff==='galaxy-drift'||eff==='deep-space')starsField(a,d,t,eff==='galaxy-drift');if(eff==='aurora'||eff==='nebula'||eff==='deep-space')aurora(a,b,c,t);if(eff==='vortex'||eff==='electric-vortex'||eff==='singularity')vortex(a,b,t);if(eff==='radar')radar(a,b,t);if(eff==='quantum-web'||eff==='ai-neural')network(a,b,c);if(eff==='data-core'||eff==='holo-rings'||eff==='reactor'||eff==='holographic')core(a,b,c,t);if(eff==='ocean'||eff==='digital-ocean')waves(a,b,t);if(eff==='hex'||eff==='hex-flux')hex(a,b);if(eff==='cyber-city')city(a,b,t);if(eff==='energy-tunnel'||eff==='warp')starsField(a,d,t,true);if(eff==='neon'||eff==='plasma'||eff==='inferno'||eff==='storm'){aurora(a,b,c,t);particleField(a,b,c,t)}if(eff==='scanlines'||eff==='holo')rainField(a,b);ctx.globalAlpha=1;ctx.globalCompositeOperation='source-over';
 if(!motion){/* render remains frozen by CSS-independent state comparison */}
 }catch(e){ctx.globalAlpha=1;ctx.globalCompositeOperation='source-over'}
 raf=requestAnimationFrame(render)
}
function addExtraCards(){const grid=$('#design .effect-grid');if(!grid||grid.dataset.v2)return;grid.dataset.v2='1';for(const [id,name,desc] of extra){const b=document.createElement('button');b.className='effect-card premium-effect';b.dataset.effect=id;b.innerHTML=`<div class="effect-preview premium-preview"><b>${name}</b></div><strong>${name}</strong><small>${desc}</small>`;grid.appendChild(b);b.onclick=()=>{const st=state();st.background=id;localStorage.setItem(KEY,JSON.stringify(st));document.body.dataset.ambience=id;document.querySelectorAll('.effect-card').forEach(x=>x.classList.toggle('active',x.dataset.effect===id));const t=$('#themeEffectName');if(t)t.textContent=name}}}
function init(){if($('#tradenexAmbience')==null){const el=document.createElement('div');el.id='tradenexAmbience';document.body.prepend(el)}const host=$('#tradenexAmbience');canvas=document.createElement('canvas');canvas.className='tradenex-ambience-canvas';host.appendChild(canvas);ctx=canvas.getContext('2d',{alpha:true});resize();window.addEventListener('resize',resize,{passive:true});setInterval(addExtraCards,500);setTimeout(addExtraCards,600);raf=requestAnimationFrame(render)}
if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',()=>setTimeout(init,500),{once:true});else setTimeout(init,500);
})();